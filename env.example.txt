# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
FRONTEND_URL=http://localhost:3000

# PostgreSQL
DB_NAME=medicore_db
DB_USER=postgres
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# Redis (Channels + Celery) — optional in dev
REDIS_URL=redis://localhost:6379/0

# Frontend (Vite)
VITE_API_URL=http://localhost:8080
VITE_TELE_WS_URL=ws://localhost:8080

# Email / Twilio — fill for production
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
